apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: fleetingdns-secret-manager
  annotations:
    config.kubernetes.io/local-config: "true"

resources:
- fleetingdns-secrets.yaml

commonLabels:
  app.kubernetes.io/name: fleetingdns-secret-manager
  app.kubernetes.io/component: secret-manager
  app.kubernetes.io/part-of: fleetingdns
  app.kubernetes.io/managed-by: crossplane

patches:
- target:
    group: "*"
    version: "*"
    kind: "*"
  patch: |-
    - op: add
      path: /metadata/annotations/crossplane.io~1managed-by
      value: fleetingdns-platform 