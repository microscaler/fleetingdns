apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: org-folders
  annotations:
    config.kubernetes.io/local-config: "true"

resources:
  - fleetingdns-folder.yaml
  - environments-folder.yaml
  - security-folder.yaml
  - analytics-folder.yaml

commonLabels:
  app.kubernetes.io/name: fleetingdns-org-folders
  app.kubernetes.io/component: organization
  app.kubernetes.io/part-of: fleetingdns-infrastructure 