apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: fleetingdns-organization
  annotations:
    config.kubernetes.io/local-config: "true"

resources:
# GCP Projects
- projects/fleetingdns-projects.yaml

commonLabels:
  app.kubernetes.io/name: fleetingdns-organization
  app.kubernetes.io/component: organization
  app.kubernetes.io/part-of: fleetingdns
  app.kubernetes.io/managed-by: crossplane

patches:
# Add common annotations to all resources
- target:
    group: "*"
    version: "*"
    kind: "*"
  patch: |-
    - op: add
      path: /metadata/annotations/crossplane.io~1managed-by
      value: fleetingdns-platform
    - op: add
      path: /metadata/annotations/fleetingdns.io~1component
      value: organization 