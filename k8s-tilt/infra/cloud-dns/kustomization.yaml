apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: fleetingdns-cloud-dns
  annotations:
    config.kubernetes.io/local-config: "true"

resources:
- fleetingdns-zones.yaml

commonLabels:
  app.kubernetes.io/name: fleetingdns-cloud-dns
  app.kubernetes.io/component: cloud-dns
  app.kubernetes.io/part-of: fleetingdns
  app.kubernetes.io/managed-by: crossplane

patches:
- target:
    group: "*"
    version: "*"
    kind: "*"
  patch: |-
    - op: add
      path: /metadata/annotations/crossplane.io~1managed-by
      value: fleetingdns-platform 