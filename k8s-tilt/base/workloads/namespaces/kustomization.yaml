apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Flux Kustomization for dependency management
  - flux-kustomization.yaml
  
  # Workload service namespaces only (external-secrets and external-dns come from control-plane)
  - services/fleetingdns.yaml 