apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Flux Kustomization for dependency management
  - flux-kustomization.yaml
  
  # Essential control-plane component namespaces (needed by both cluster types)
  - components/crossplane.yaml
  - components/external-secrets.yaml
  - components/external-dns.yaml 