apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Control plane components (infrastructure, observability, platform)
  - control-plane/
  
  # Workload components (FleetingDNS services)
  - workloads/ 