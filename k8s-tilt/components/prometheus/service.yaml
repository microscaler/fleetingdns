apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: fleetingdns
  labels:
    app: prometheus
    component: observability
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: 9090
      protocol: TCP
      name: http
  selector:
    app: prometheus 