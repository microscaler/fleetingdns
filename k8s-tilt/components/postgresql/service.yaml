apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: fleetingdns
  labels:
    app: postgres
    component: infrastructure
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: postgres 