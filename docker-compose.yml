

x-rust-build: &rust-build
  context: .
  target: runtime

services:
  dnsd:
    build:
      <<: *rust-build
      dockerfile: docker/Dockerfile.dnsd
    image: fleetingdns/dnsd:dev
    command: ["/app/dnsd-bin", "--addr", "0.0.0.0:53"]
    ports: ["6353:53/udp"]
    depends_on: [otel-collector]

  edgehub:
    build:
      <<: *rust-build
      dockerfile: docker/Dockerfile.edgehub
    image: fleetingdns/edgehub:dev
    command: ["/app/edgehub-bin"]
    ports: ["2222:2222"]
    depends_on: [dnsd, otel-collector]

  intake_collector:
    build:
      <<: *rust-build
      dockerfile: docker/Dockerfile.intake_collector
    image: fleetingdns/intake_collector:dev
    command: ["/app/intake_collector-bin"]
    depends_on: [otel-collector]

  ml_scorer:
    build:
      <<: *rust-build
      dockerfile: docker/Dockerfile.ml_scorer
    image: fleetingdns/ml_scorer:dev
    command: ["/app/ml_scorer-bin"]
    depends_on: [otel-collector]

  feed_grpc:
    build:
      <<: *rust-build
      dockerfile: docker/Dockerfile.feed_grpc
    image: fleetingdns/feed_grpc:dev
    command: ["/app/feed_grpc-bin"]
    depends_on: [otel-collector]

  feed_webhook:
    build:
      <<: *rust-build
      dockerfile: docker/Dockerfile.feed_webhook
    image: fleetingdns/feed_webhook:dev
    command: ["/app/feed_webhook-bin"]
    depends_on: [otel-collector]

  api:
    build:
      <<: *rust-build
      dockerfile: docker/Dockerfile.api
    image: fleetingdns/api:dev
    command: ["/app/api-bin"]
    environment:
      - DATABASE_URL=postgresql://fdns:fdns@postgres:5432/fdns
      - REDIS_URL=redis://redis:6379
      - RUST_LOG=info
    depends_on: [postgres, redis, otel-collector]

  redis:
    image: redis:7-alpine
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    ports: ["6379:6379"]

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: fdns
      POSTGRES_PASSWORD: fdns
      POSTGRES_DB: fdns
    volumes: ["pgdata:/var/lib/postgresql/data"]

  mimir:
    image: grafana/mimir:2.10.2
    command: ["-config.file=/etc/mimir.yaml"]
    volumes: ["./docker/mimir-local.yaml:/etc/mimir.yaml:ro"]

  otel-collector:
    image: otel/opentelemetry-collector-contrib:0.93.0
    volumes:
      - ./docker/otel-collector-config.yaml:/etc/otel/config.yaml:ro
    command: ["--config=/etc/otel/config.yaml"]
    ports:
      - "4317:4317"
      - "4318:4318"

  prometheus:
    image: prom/prometheus:v2.52.0
    volumes:
      - ./docker/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--enable-feature=memory-snapshot-on-shutdown"
    ports: ["9090:9090"]

  loki:
    image: grafana/loki:2.9.3
    command: ["-config.file=/etc/loki-config.yaml"]
    volumes:
      - ./docker/loki-config.yaml:/etc/loki-config.yaml:ro
    ports: ["3100:3100"]

  promtail:
    image: grafana/promtail:2.9.3
    command: ["-config.file=/etc/promtail-config.yaml"]
    volumes:
      - ./docker/promtail-config.yaml:/etc/promtail-config.yaml:ro
      - /var/log:/var/log:ro

  grafana:
    image: grafana/grafana:10.3.1
    volumes:
      - ./docker/grafana/grafana.ini:/etc/grafana/grafana.ini:ro
      - ./docker/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./docker/grafana/dashboards:/var/lib/grafana/dashboards:ro
    environment:
      - GF_SERVER_HTTP_PORT=3000
    ports: ["3000:3000"]
    depends_on: [prometheus, loki, mimir]

volumes:
  pgdata:

networks:
  default:
    driver: bridge
